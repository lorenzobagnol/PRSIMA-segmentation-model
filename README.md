# daniele-panerai

### Run locally

You can install this via docker container. You should comment out the CUDA lines of the requirements.txt before starting if you are not using a CUDA machine.
Then yu can build and run the container using the local-dockerfile.dockerfile and it should work like a charm.

### Run on Google Cloud Platform

You have to build an Artifact Registry using the cloudbuild.yaml